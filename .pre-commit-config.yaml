default_language_version:
  python: python3.13
repos:
- repo: https://github.com/astral-sh/ruff-pre-commit
  rev: v0.15.1
  hooks:
    # Run the linter.
    - id: ruff
      args: [ --fix-only ]
    # Run the formatter.
    - id: ruff-format
